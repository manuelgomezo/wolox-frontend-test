const configJSON = {
  IMAGE_PROVIDER: 'https://s3.amazonaws.com/cdn.wolox.com/',
  WOLOX_API: 'http://private-8e8921-woloxfrontendinverview.apiary-mock.com',
};

const config = Object.freeze(configJSON);

export default config;
